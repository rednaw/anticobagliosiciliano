{
  "name": "anticobagliosiciliano",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=${localEnv:HOME}/.ssh,target=/root/.ssh,type=bind,readonly,consistency=cached",
    "source=${localEnv:HOME}/.gitconfig,target=/root/.gitconfig,type=bind,readonly,consistency=cached"
  ],
  "postCreateCommand": "npm ci && npm ci --prefix archive/browser",
  "forwardPorts": [5173, 5174],
  "portsAttributes": {
    "5173": {
      "label": "Marketing site (Vite)",
      "onAutoForward": "notify"
    },
    "5174": {
      "label": "Archivio (local only)",
      "onAutoForward": "silent"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["svelte.svelte-vscode", "editorconfig.editorconfig", "repreng.csv"],
      "settings": {
        "editor.insertSpaces": true,
        "editor.tabSize": 2,
        "editor.detectIndentation": false
      }
    }
  }
}

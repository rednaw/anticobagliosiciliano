/** Hostname registered in Simple Analytics (must match `data-hostname` in `app.html`). */
export const SIMPLE_ANALYTICS_HOSTNAME = 'anticobagliosiciliano.rednaw.github.io';

/** Flip to `true` on launch so search engines can index the site. */
export const SITE_PUBLIC = false;
